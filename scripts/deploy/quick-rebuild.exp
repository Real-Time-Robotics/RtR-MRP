#!/usr/bin/expect -f
set timeout 900
spawn ssh lam@171.244.40.23
expect "password:" { send "Tranthiquynh1987@\r" }
expect "\\$"

send "cd /home/lam/RtR-MRP && git pull origin main && docker compose build app --no-cache && docker compose up -d --no-deps app --force-recreate && sleep 20 && curl -sf https://mrp.rtrobotics.com/api/health && echo ' XDONE'\r"
expect {
    "XDONE" { puts "\nDEPLOY OK" }
    timeout { puts "\nTIMEOUT" }
}
expect "\\$"
send "exit\r"
expect eof
