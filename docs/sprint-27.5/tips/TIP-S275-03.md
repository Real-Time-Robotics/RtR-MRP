# TIP-S275-03 · X-Ray full app audit

## HEADER
- **ID:** TIP-S275-03
- **Branch:** `fix/sprint-27.5-sidebar-routes-xray`
- **Depends on:** TIP-S275-01 + TIP-S275-02 (sidebar fix done first).
- **Blocks:** none — out là report, không phải code change.
- **Priority:** P1
- **Commit message:** `chore(audit): X-Ray full app audit report (TIP-S275-03)`

## CONTEXT

Sprint 27 sidebar v2 phát hiện 18 link gãy. Chưa biết toàn app còn gãy ở đâu — top-nav header (`HOME OPERATIONS PRODUCTION QUALITY ANALYTICS`) có ~30 link, deep link Sprint 27 (`/dashboard/search/serial/<serial>`), button trong card list dữ liệu, redirect sau action — chưa scan.

TIP này là **audit-only**: scan toàn bộ navigation → list bug → ưu tiên → recommendation. KHÔNG fix (Sprint 28+ fix tuỳ priority).

## TASK

### 1) Scan navigation primary

**Top-nav header** (`src/components/layout/header/header-nav-data.tsx`):
- Liệt kê tất cả `href:` trong file (5 tab × ~6 sub = ~30 link).
- Đối chiếu route `(dashboard)/` tồn tại không.
- Output table 30 row × {href, target, exists, recommendation}.

**Sidebar v2** (`src/components/layout/sidebar-v2.tsx`) — sau TIP-01+02 fix:
- Re-scan 40 link → confirm 0 link 404.
- Output table verify pass.

**SidebarLegacy** (`src/components/layout/minimalist-sidebar.tsx`):
- Liệt kê href trong sidebar cũ (gateway user vẫn fallback dùng cái này).
- Audit 26 entry navigation cũ.

### 2) Scan in-page navigation

Scan toàn bộ `src/app/(dashboard)/**/*.tsx` + `src/components/**/*.tsx`:

```bash
grep -rEn 'href="/[^"]*"|href=\{[`'"'"']/[^"]*' src/app src/components | head -200
grep -rEn 'router\.push\([`'"'"']/[^"]*' src/app src/components | head -100
```

Liệt kê unique href + đối chiếu route tồn tại.

Phân loại:
- **CRITICAL:** href trong main user flow (Operations, Production, Tra cứu, Mua hàng) — gãy = block adoption.
- **HIGH:** href trong card detail (vd "Xem WO" trỏ `/work-orders/<id>` mà route không có).
- **MEDIUM:** href trong report list, dashboard widget.
- **LOW:** href trong settings, helper.

### 3) Scan deep link Sprint 27

- `/dashboard/search/serial/<serial>` (TIP-S27-08 server component) — verify render với serial test có sẵn DB.
- `/dashboard/operations/assembly/<id>` (TIP-S27-07) — verify render.
- API deep link nếu UI gọi: `/api/serial/<serial>`, `/api/assembly/<id>` — chạy curl với session cookie test.

### 4) Scan auth-gate + role-gate

- Login user role `viewer` → click qua sidebar v2 → ghi nhận route nào trả 403 (expected) vs 500 (bug) vs render trống (logic gap).
- Login user role `production` → access route role-gate `engineer` (vd `/engineering/module-design`) → expected 403 hoặc redirect.
- Login user **không role nào** → expected fallback `MinimalistSidebar` (commit `5533303` graceful), nhưng route role-gate vẫn 403.

### 5) Output report

File: `docs/sprint-27.5/XRAY_REPORT.md`

```markdown
# Sprint 27.5 · X-Ray Audit Report

**Date:** YYYY-MM-DD
**Scope:** sidebar v2 + sidebar legacy + top-nav header + in-page link + deep link + role gate
**Total link scanned:** N

## Summary

| Severity | Count |
|---|---|
| CRITICAL | X |
| HIGH | Y |
| MEDIUM | Z |
| LOW | W |
| OK | M |

## CRITICAL Issues
| # | Source | Link | Expected | Actual | Recommendation |
|---|---|---|---|---|---|
| 1 | top-nav | /production/oee | 200 | 404 | tạo skeleton hoặc redirect |
| ... | ... | ... | ... | ... | ... |

## HIGH Issues
...

## Recommendations Sprint 28+

### Quick wins (Sprint 28 Wave 0, <1 ngày):
- Fix critical link X (đổi href hoặc tạo skeleton).
- ...

### Defer Sprint 30:
- ...
```

### 6) Bonus quality checks (nếu thời gian)

- **Performance regression check:** vài route Sprint 27 mới có Lighthouse score thấp không? `npx lighthouse mrp.rtrobotics.com/operations/assembly --output json --quiet | jq .categories.performance.score`. Optional.
- **Console error scan:** chạy `npm run dev` + click qua sidebar v2 6 cụm → ghi console error nếu có.
- **Bundle size:** `npm run build` → compare bundle size trước/sau Sprint 27. Optional.

## ACCEPTANCE

- [ ] `XRAY_REPORT.md` đầy đủ 5 section.
- [ ] Top-nav header: 30 link scanned + categorized.
- [ ] Sidebar v2: 40 link verified 0 broken (sau TIP-01+02).
- [ ] In-page: ≥ 50 link unique scanned từ src/app.
- [ ] Deep link: 2 case Sprint 27 verified.
- [ ] Role gate: 3 role smoke (viewer/production/no-role).
- [ ] Recommendation list rõ ràng: critical fix Sprint 28 Wave 0 + defer.

## CONSTRAINTS

- KHÔNG fix code trong TIP này — chỉ audit + report.
- KHÔNG yêu cầu CI run — chạy local đủ.
- Manual + grep + curl đủ (không cần Playwright suite mới).
- Time-box: 4-6h. Nếu vượt → cắt scope (vd skip bonus performance check).

## COMPLETION REPORT

```
STATUS: DONE
BRANCH: fix/sprint-27.5-sidebar-routes-xray
COMMIT: <hash> chore(audit): X-Ray full app audit report (TIP-S275-03)

FILES CREATED:
  - docs/sprint-27.5/XRAY_REPORT.md

AUDIT SCOPE:
  - Top-nav header:    30 link (X OK · Y broken)
  - Sidebar v2:        40 link (40 OK after TIP-01+02 ✓)
  - Sidebar legacy:    26 link (X OK · Y broken)
  - In-page link:      N link unique
  - Deep link:         2/2 verified
  - Role gate:         3 role tested

ISSUES FOUND:
  CRITICAL: <count>
  HIGH:     <count>
  MEDIUM:   <count>
  LOW:      <count>

RECOMMENDATIONS:
  Sprint 28 Wave 0 (quick win): <list>
  Sprint 30 defer:              <list>

DEVIATIONS: <list or "none">

Sprint 27.5 done. Chờ Lâm review XRAY_REPORT + decide next.
```
