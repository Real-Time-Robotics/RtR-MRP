# TIP-S275-01 · Sidebar v2 path correction

## HEADER
- **ID:** TIP-S275-01
- **Branch:** `fix/sprint-27.5-sidebar-routes-xray` (tách từ `main`)
- **Depends on:** Sprint 27 sidebar v2 (`fc9e6e0`).
- **Runs parallel with:** TIP-S275-02 (skeleton page — không đụng cùng file logic).
- **Blocks:** none.
- **Priority:** P0
- **Commit message:** `fix(ui): sidebar v2 path correction (TIP-S275-01)`

## CONTEXT

Sprint 27 viết sidebar v2 config theo "best-guess" path → 8 href trỏ sai route thực có. Chủ thầu đã scan ra mapping. TIP này áp đúng path.

**File chính:** `src/components/layout/sidebar-v2.tsx`

## TASK

### 1) Update href trong `SIDEBAR_GROUPS`

| Cũ (sai) | Mới (đúng route hiện có) |
|---|---|
| `/purchase-orders` | `/purchasing` |
| `/grn` | `/purchasing/grn` ← tạo skeleton TIP-02 nếu chưa có |
| `/admin/audit` | `/audit` |
| `/warehouse/locations` | `/warehouses` |
| `/warehouse-issues` | `/inventory/issue` |
| `/admin/users` | giữ — TIP-02 tạo skeleton |
| `/admin/roles` | giữ — TIP-02 tạo skeleton |
| `/warehouse-receipts` | giữ — TIP-02 tạo skeleton |

5 path đổi trong sidebar v2 + 3 path giữ (TIP-02 sẽ skeleton).

### 2) Update label tiếng Việt nếu cần

- "Vị trí" cụm Kho → có thể đổi "Kho hàng" cho phù hợp `/warehouses`.
- "Xuất kho" giữ → trỏ `/inventory/issue`.

### 3) Update test matrix

`src/components/layout/__tests__/sidebar-v2.test.tsx`:
- Thay assertion href trong test cũ.
- Thêm test: snapshot tất cả href trong `SIDEBAR_GROUPS` không match pattern `/purchase-orders|/grn|/admin/audit|/warehouse/locations|/warehouse-issues` (path cũ sai đã loại).

## ACCEPTANCE

- [ ] `tsc --noEmit` 0 error.
- [ ] `lint` không tăng warning.
- [ ] Sidebar test pass (số test ≥ 13 Sprint 27).
- [ ] Manual: `npm run dev` + login admin → 5 path đổi trỏ đúng route hiện có (không 404 cho 5 path này).
- [ ] 3 path còn lại (`/admin/users`, `/admin/roles`, `/warehouse-receipts`) vẫn 404 — đợi TIP-02.

## CONSTRAINTS

- KHÔNG đổi cấu trúc cụm (giữ 9 cụm).
- KHÔNG đổi role gate.
- KHÔNG đụng SidebarLegacy.

## COMPLETION REPORT

```
STATUS: DONE
BRANCH: fix/sprint-27.5-sidebar-routes-xray
COMMIT: <hash> fix(ui): sidebar v2 path correction (TIP-S275-01)

FILES MODIFIED:
  - src/components/layout/sidebar-v2.tsx (5 href update)
  - src/components/layout/__tests__/sidebar-v2.test.tsx

PATHS CORRECTED:
  /purchase-orders     → /purchasing
  /grn                 → /purchasing/grn
  /admin/audit         → /audit
  /warehouse/locations → /warehouses
  /warehouse-issues    → /inventory/issue

PATHS DEFERRED to TIP-02 (will be skeleton):
  /admin/users · /admin/roles · /warehouse-receipts

TESTS: X/X pass
TYPECHECK: 0 error
LINT: no new warning

DEVIATIONS: <list or "none">

Wave 1 partial — TIP-01 done. Chờ TIP-02 + TIP-03.
```
