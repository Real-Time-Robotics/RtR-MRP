# TIP-S275-02 · Tạo skeleton missing pages

## HEADER
- **ID:** TIP-S275-02
- **Branch:** `fix/sprint-27.5-sidebar-routes-xray`
- **Depends on:** Sprint 27 (skeleton pattern Sprint 27 đã có).
- **Runs parallel with:** TIP-S275-01 (path correction — không đụng cùng file).
- **Blocks:** none.
- **Priority:** P0
- **Commit message:** `feat(ui): skeleton pages for sidebar v2 routes (TIP-S275-02)`

## CONTEXT

14 route bị sidebar v2 link nhưng chưa có page → 404. Tạo skeleton cho mỗi route. Skeleton: heading + 1 dòng mô tả + back link. KHÔNG implement business logic — Sprint 28-30 sẽ fill.

**Pattern reference:** `src/app/(dashboard)/my-work/page.tsx` (Sprint 27 đã có skeleton).

## TASK

### 1) Tạo 14 file skeleton

| File path | Title | Mô tả ngắn |
|---|---|---|
| `src/app/(dashboard)/my-work/created/page.tsx` | Lệnh tôi tạo | Danh sách lệnh do tôi khởi tạo. |
| `src/app/(dashboard)/my-work/approvals/page.tsx` | Approval | Danh sách yêu cầu chờ tôi duyệt. |
| `src/app/(dashboard)/search/lot-history/page.tsx` | Lịch sử lô | Truy xuất lịch sử nhập/xuất theo lô. |
| `src/app/(dashboard)/engineering/module-design/page.tsx` | Module Design | Quản lý ModuleDesign + numbering rule. |
| `src/app/(dashboard)/engineering/bom/page.tsx` | BOM Templates | Quản lý template BOM. |
| `src/app/(dashboard)/engineering/numbering/page.tsx` | Numbering Rules | Quản lý SerialNumberingRule. |
| `src/app/(dashboard)/purchase-requests/page.tsx` | Yêu cầu mua | Danh sách Purchase Request. |
| `src/app/(dashboard)/reports/low-stock/page.tsx` | Tồn kho thấp | Báo cáo tồn kho dưới ngưỡng. |
| `src/app/(dashboard)/reports/production/page.tsx` | Sản lượng | Báo cáo sản lượng theo ngày/tuần/tháng. |
| `src/app/(dashboard)/reports/serial/page.tsx` | Serial throughput | Throughput serial sản xuất. |
| `src/app/(dashboard)/admin/users/page.tsx` | Người dùng | Quản lý người dùng + role assign. |
| `src/app/(dashboard)/admin/roles/page.tsx` | Roles | Quản lý role + permission. |
| `src/app/(dashboard)/admin/audit/page.tsx` | Audit Log | Lịch sử thao tác hệ thống. (Hoặc redirect `/audit` đã có) |
| `src/app/(dashboard)/warehouse-receipts/page.tsx` | Nhận hàng | Phiếu nhập kho từ supplier. |
| `src/app/(dashboard)/purchasing/grn/page.tsx` | GRN | Goods Receipt Note. |
| `src/app/(dashboard)/mobile/page.tsx` | Mobile Dashboard | Trang chủ mobile (sub-route hiện có sẵn). |

Tổng: **15 file** (12 skeleton + 1 redirect/audit + 1 grn nested + 1 mobile index).

### 2) Skeleton template

```tsx
import Link from 'next/link';
import { ArrowLeft } from 'lucide-react';

export default function Page() {
  return (
    <div className="p-6 max-w-3xl">
      <Link href="/home" className="inline-flex items-center text-sm text-muted-foreground hover:text-foreground mb-4">
        <ArrowLeft className="w-4 h-4 mr-1" /> Quay về trang chủ
      </Link>
      <h1 className="text-2xl font-semibold mb-2">[Title]</h1>
      <p className="text-muted-foreground mb-6">[Mô tả ngắn 1 dòng]</p>
      <div className="rounded-lg border bg-muted/30 p-6 text-center">
        <p className="text-sm text-muted-foreground">
          Trang này sẽ được implement Sprint 28-30. Theo dõi tiến độ ở docs/sprint-28+.
        </p>
      </div>
    </div>
  );
}
```

### 3) Special case: `/admin/audit` redirect

Route `(dashboard)/audit/` đã tồn tại (Sprint trước). Không tạo skeleton cho `/admin/audit` — thay vào đó:

```tsx
// src/app/(dashboard)/admin/audit/page.tsx
import { redirect } from 'next/navigation';
export default function AdminAuditRedirect() {
  redirect('/audit');
}
```

Hoặc TIP-01 đã đổi href `/admin/audit` → `/audit` trong sidebar v2. Nếu vậy KHÔNG cần file này. Verify với TIP-01 commit trước khi tạo.

### 4) Tests

Tạo 1 file test gộp `src/app/__tests__/skeleton-routes.test.tsx`:
- Mỗi route render được, không crash.
- Heading hiển thị đúng.
- Back link tới `/home`.

Hoặc skip test — skeleton page logic 0%, test coverage không cần. Chấp nhận theo judgment Thợ.

## ACCEPTANCE

- [ ] 15 file (hoặc 14 nếu bỏ admin/audit redirect) được tạo.
- [ ] `tsc --noEmit` 0 error.
- [ ] `lint` không tăng warning.
- [ ] Manual: `npm run dev` + click 14 sidebar link mới (sau TIP-01 path correction) → 0 link 404. Mỗi page render heading + back link.
- [ ] Mobile responsive (375px) — skeleton template đã responsive sẵn.

## CONSTRAINTS

- KHÔNG implement business logic.
- KHÔNG đụng API route.
- KHÔNG đụng schema.
- Reuse `lucide-react` icon (đã có dep).
- i18n tiếng Việt cho heading + mô tả.
- Skeleton page có thể dùng "use client" hoặc server component — chọn cái nào đơn giản hơn (server component).

## COMPLETION REPORT

```
STATUS: DONE
BRANCH: fix/sprint-27.5-sidebar-routes-xray
COMMIT: <hash> feat(ui): skeleton pages for sidebar v2 routes (TIP-S275-02)

FILES CREATED: <list 14-15 file>

TESTS: <X/X pass hoặc skip>
TYPECHECK: 0 error
LINT: no new warning

MANUAL VERIFY:
  - 14 sidebar link click không 404 ✓
  - Heading + back link render ✓

DEVIATIONS: <list or "none">

Wave 1 done sau TIP-01 + TIP-02. Chờ TIP-03 X-Ray full app.
```
