# TIP-S27-08 · Tra cứu Serial + route hiding

## HEADER
- **ID:** TIP-S27-08
- **Branch:** `feat/sprint-27-electronics-ia` (tiếp từ commit `fc9e6e0`)
- **Depends on:** TIP-S27-02 (`2fb0129`) — `/api/serial/:serial` đã có. TIP-S27-06 (`fc9e6e0`) — sidebar v2 + skeleton `/dashboard/search`.
- **Runs parallel with:** TIP-S27-07 (Vận hành pages) — không đụng cùng file.
- **Priority:** P0 (Tra cứu) + P1 (Route hiding)
- **REQ-ID cover:** REQ-27-13, REQ-27-14
- **Commit message:** `feat(ui): tra cứu serial + route hiding via feature flags (TIP-S27-08)`

---

## CONTEXT

**Working dir:** `/Users/os/RtR/rtr-mrp-pr`

**Tại sao TIP này:**
2 mục tiêu độc lập gộp 1 commit (cả 2 đều P0/P1 nhỏ, cùng động chạm sidebar v2):
1. **Tra cứu Serial** — search box unified: gõ/quét serial → trả full info (parent, children, history). Đây là tool khắc phục Excel SERIAL_MASTER hiện tại của phòng điện tử.
2. **Route hiding** — ẩn 5 cụm route khỏi sidebar (Finance, AI/ML, Multi-tenant, Mobile, Compliance). KHÔNG xoá route, chỉ hide khỏi menu chính. Admin có toggle bật lại từng cụm qua Feature Flags page.

**Key files đọc trước:**
- `src/app/api/serial/[serial]/route.ts` — API contract TIP-02. Đọc field response để biết shape.
- `src/app/(dashboard)/search/page.tsx` — skeleton TIP-06.
- `src/components/layout/sidebar-v2.tsx` — group config + filter logic. Sửa để filter theo feature flag thêm.
- `src/lib/feature-flags.ts` — TIP-06 đã thêm `SIDEBAR_V2`. Mở rộng cho 5 flag mới.
- `prisma/schema.prisma` — `LotTransaction`, `ScanLog` (nếu có) cho history timeline.

---

## TASK

### 1) Trang `/dashboard/search/serial/page.tsx` — Tra cứu Serial chính

**File:** `src/app/(dashboard)/search/serial/page.tsx`

**Layout:**
- Header: "Tra cứu Serial" + sub-text "Quét hoặc nhập serial để xem thông tin đầy đủ."
- Search bar lớn (autofocus): input + button "Tra cứu" (Enter cũng submit).
  - Giữ history 5 search gần nhất ở localStorage hoặc state.
- Sau submit: gọi `GET /api/serial/<serial>`.
- Loading skeleton.
- Result card:
  - **Header:** Serial + status badge color-coded (IN_STOCK xanh, ALLOCATED vàng, CONSUMED tím, SHIPPED xám, SCRAPPED đỏ, RETURNED cam).
  - **Grid 2 cột:**
    - Cột trái: Product · Module Design · Part · Source · Created By · Created At · Inventory location · Warehouse + locationCode.
    - Cột phải: Notes · Meta JSON (collapsible).
  - **Section Parent:** nếu `parentLinks[0]` có → render card link `/dashboard/search/serial/<parentSerial>` + button "Xem cha".
  - **Section Children:** nếu `childLinks` không rỗng:
    - Render expandable list. Default collapsed.
    - Khi expand: table N children với cột Serial · Product · Status · Action ("Mở").
    - Pagination 25/page nếu nhiều.
  - **Section Timeline:**
    - Gọi optional `GET /api/serial/<serial>/history` (nếu chưa có endpoint, ghi TODO + skip cho TIP này).
    - Hoặc query `LotTransaction.findMany({where: {parentLots: {path: ['serial'], equals: serial}}})` qua endpoint mới — nếu schema không support thì fallback chỉ display SerialUnit.createdAt + updatedAt + status history từ `meta.statusHistory` (TIP-02 ghi vào).
- Error state: 404 → "Không tìm thấy serial". 5xx → "Lỗi tra cứu, thử lại."

**URL routing:**
- `/dashboard/search/serial` — empty search.
- `/dashboard/search/serial?q=<serial>` — auto-trigger search.
- `/dashboard/search/serial/<serial>` (dynamic route file `[serial]/page.tsx`) — direct deep link, server-side fetch.

**File phụ:** `src/app/(dashboard)/search/serial/[serial]/page.tsx` server component fetch `/api/serial/<serial>` ở server, render cùng component card. SEO + share-able URL.

### 2) Component dùng chung

**File:** `src/components/serial/serial-detail-card.tsx`

Props: `{ serial: SerialResponse }` (kiểu từ API contract).

Render đầy đủ result card. Reuse cho cả `[serial]/page.tsx` (server) và `serial/page.tsx` (client search submit). Tách hẳn để 2 chỗ cùng dùng.

### 3) Performance & target

- Tra cứu serial → response render trong **<500ms** (target Blueprint §TIP-08 acceptance).
- API TIP-02 `GET /api/serial/:serial` đã có index trên `serial` (unique). Tăng tốc thêm bằng:
  - Server component `[serial]/page.tsx` Next.js cache theo serial: `revalidate = 60` hoặc on-demand `revalidatePath` khi status đổi.
  - Hoặc client component fetch direct, không cần Next cache (đơn giản, chấp nhận).

### 4) Route hiding qua feature flag

**Mở rộng `src/lib/feature-flags.ts`:**

```ts
export const FEATURE_FLAGS = {
  SIDEBAR_V2: process.env.NEXT_PUBLIC_SIDEBAR_V2 !== 'false',  // TIP-06

  // TIP-08: hide 5 cụm khỏi sidebar default. Set true để bật lại từng cụm.
  SHOW_FINANCE:      process.env.NEXT_PUBLIC_SHOW_FINANCE      === 'true',
  SHOW_AI_ML:        process.env.NEXT_PUBLIC_SHOW_AI_ML        === 'true',
  SHOW_MULTITENANT:  process.env.NEXT_PUBLIC_SHOW_MULTITENANT  === 'true',
  SHOW_MOBILE:       process.env.NEXT_PUBLIC_SHOW_MOBILE       === 'true',
  SHOW_COMPLIANCE:   process.env.NEXT_PUBLIC_SHOW_COMPLIANCE   === 'true',
} as const;
```

**Cập nhật `sidebar-v2.tsx`:**

Cụm "Báo cáo" (8) hiện có sub-menu thuần. KHÔNG đụng cụm này.

KIỂM TRA: `SidebarLegacy` (`minimalist-sidebar.tsx`) có thể đang link sang Finance/AI/etc. Nếu `SIDEBAR_V2=true` mặc định thì legacy không hiển thị → không cần đụng. Nhưng vẫn giữ legacy nguyên (rollback path).

**Trong `sidebar-v2.tsx`** — nếu hiện đã có cụm Finance/AI/ML/Multi-tenant/Mobile/Compliance trong group config (theo TIP-06 thì KHÔNG có vì chỉ có 9 cụm chuẩn). Verify lại: nếu chưa có thì TIP-08 không cần edit sidebar.

→ **Quyết định:** sidebar v2 từ TIP-06 đã KHÔNG có 5 cụm trên. → Nhiệm vụ TIP-08 ở đây chỉ là:
- a) Giữ feature flag chung trong codebase để Admin Feature Flags page bật lại được.
- b) Trang Admin Feature Flags hiển thị 5 toggle.
- c) Khi flag bật → 5 cụm xuất hiện thêm trong sidebar v2.

→ **Update SIDEBAR_GROUPS in sidebar-v2.tsx:** thêm 5 cụm mới vào array, mỗi cụm có field `featureFlag?: keyof FEATURE_FLAGS`. Filter logic mở rộng:

```ts
const groups = SIDEBAR_GROUPS.filter(g => 
  (!g.featureFlag || FEATURE_FLAGS[g.featureFlag]) &&
  (g.roles === 'all' || userRoles.some(r => g.roles.includes(r)))
);
```

5 cụm thêm:
```ts
{ id: 'finance',     label: 'Tài chính',  icon: DollarSign, roles: ['admin'],
  featureFlag: 'SHOW_FINANCE',
  items: [
    { href: '/dashboard/finance/invoices', label: 'Hoá đơn' },
    { href: '/dashboard/finance/payments', label: 'Thanh toán' },
  ] },
{ id: 'ai-ml',       label: 'AI / ML',    icon: Brain, roles: ['admin'],
  featureFlag: 'SHOW_AI_ML',
  items: [
    { href: '/dashboard/ai/forecast',    label: 'Forecast' },
    { href: '/dashboard/ml/anomaly',     label: 'Anomaly' },
  ] },
{ id: 'multitenant', label: 'Multi-tenant', icon: Building, roles: ['admin'],
  featureFlag: 'SHOW_MULTITENANT',
  items: [
    { href: '/dashboard/tenants',          label: 'Tenants' },
    { href: '/dashboard/subscription',    label: 'Subscription' },
  ] },
{ id: 'mobile',      label: 'Mobile',     icon: Smartphone, roles: ['admin'],
  featureFlag: 'SHOW_MOBILE',
  items: [
    { href: '/dashboard/mobile',           label: 'Mobile dashboard' },
  ] },
{ id: 'compliance',  label: 'Compliance', icon: Shield, roles: ['admin'],
  featureFlag: 'SHOW_COMPLIANCE',
  items: [
    { href: '/dashboard/compliance',       label: 'Compliance overview' },
  ] },
```

(Test với mỗi route — nếu route không tồn tại thì link sẽ 404. Đây là acceptable: route đã tồn tại trong repo theo Blueprint §REQ-27-14 — TIP-08 KHÔNG xoá. Verify qua grep route đó còn tồn tại trước khi link.)

### 5) Trang Admin Feature Flags

**File:** `src/app/(dashboard)/admin/feature-flags/page.tsx`

**Layout:**
- Header: "Cờ tính năng" + sub-text "Bật/tắt cụm chức năng cho người dùng. Yêu cầu restart server để áp dụng."
- Card cho mỗi flag (5 cờ TIP-08 + flag SIDEBAR_V2):
  - Tên flag · mô tả · current value (badge ON/OFF) · checkbox toggle.
  - Toggle gọi API admin (xem dưới) update `.env` hoặc database setting.

**Quyết định triển khai:**
- **Đơn giản nhất (chấp nhận MVP):** flag chỉ đọc từ `process.env`. Trang Feature Flags hiển thị **read-only**. Toggle yêu cầu admin sửa `.env` rồi restart server. Có note: "Để thay đổi: edit `.env.local` và restart."
- **Tốt hơn (nice-to-have):** flag lưu trong DB qua model `SystemSetting(key, value)`. Toggle update DB + invalidate cache. Cần migration mới — out of scope TIP-08, ghi TODO.

**MVP:** read-only display. Admin tự sửa `.env`.

### 6) Update sidebar v2 default expansion

Khi load lần đầu, role admin → expand cụm "Quản trị" (đã có ở TIP-06). Đảm bảo Feature Flags hiển thị trong sub-menu Quản trị nếu chưa có.

`SIDEBAR_GROUPS[8]` (Quản trị) sub-items:
```
Người dùng · Roles · Feature Flags · Audit Log
```
Nếu Feature Flags chưa có trong list TIP-06 → thêm.

### 7) Tests

**`src/app/(dashboard)/search/serial/__tests__/page.test.tsx`** (≥4):
1. Render search box, submit → API gọi đúng.
2. Result render đầy đủ field.
3. Parent serial link mở trang `/dashboard/search/serial/<parent>`.
4. Children expand → list render.

**`src/components/serial/__tests__/serial-detail-card.test.tsx`** (≥3):
1. Status badge đúng color cho từng SerialStatus.
2. Children section ẩn khi `childLinks` rỗng.
3. Meta JSON collapsible.

**`src/app/(dashboard)/admin/feature-flags/__tests__/page.test.tsx`** (≥2):
1. Render 6 flag với current value đọc từ FEATURE_FLAGS.
2. Note hướng dẫn restart server hiển thị.

**`src/components/layout/__tests__/sidebar-v2.test.tsx`** (mở rộng từ TIP-06):
- Thêm 3 test:
  - Admin + flag SHOW_FINANCE=false → không thấy cụm Tài chính.
  - Admin + flag SHOW_FINANCE=true (mock env) → thấy.
  - Engineer (không phải admin) + flag SHOW_FINANCE=true → vẫn không thấy (role gate ưu tiên).

---

## ACCEPTANCE

- [ ] `npx tsc --noEmit` 0 error mới.
- [ ] `npm run lint` không tăng warning.
- [ ] Test mới + extended pass:
  - search/serial: ≥4
  - serial-detail-card: ≥3
  - admin/feature-flags: ≥2
  - sidebar-v2 mở rộng: +3
  - Total: ≥12 (TIP-08) + đủ test cũ TIP-06 không bị break.
- [ ] Manual E2E:
  - `/dashboard/search/serial` → gõ `IO1-V15-XXXXXX-001` (1 serial thật từ DASHBOARD migration) → render trong <1s.
  - Kết quả đủ Product, Module, Status, Children.
  - Click parent link (nếu có) → mở trang serial cha.
  - Direct URL `/dashboard/search/serial/IO1-V15-...` (deep link) → load đúng.
  - `/dashboard/admin/feature-flags` → 6 flag hiển thị, mô tả + restart note.
  - Set `NEXT_PUBLIC_SHOW_FINANCE=true` + restart → cụm Tài chính xuất hiện trong sidebar admin.
  - Set lại `=false` → cụm ẩn.

---

## CONSTRAINTS

- **KHÔNG** xoá route Finance/AI/ML/Multi-tenant/Mobile/Compliance — chỉ hide menu. Truy cập trực tiếp URL vẫn load.
- **KHÔNG** đụng API contract TIP-02. Nếu cần endpoint history mới → chỉ ghi TODO, không tự ý tạo.
- **KHÔNG** đổi cách feature flag hoạt động (env-based). Persist qua DB là Sprint 28.
- **KHÔNG** đụng SidebarLegacy.
- **KHÔNG** tự ý thêm package. Reuse Shadcn primitives + lucide icons (Brain, DollarSign, Building, Smartphone, Shield).
- **i18n:** UI tiếng Việt.
- **A11y:** search input có label, dialog focus trap, esc đóng.
- Performance: trang `/dashboard/search/serial/[serial]` (server component) phải fetch dưới 500ms với DB local có ~280 serial. Nếu chậm → check N+1 query trong `/api/serial/:serial`.
- Ambiguity nhỏ → tự quyết + ghi DEVIATIONS.

---

## COMPLETION REPORT FORMAT

```
STATUS: DONE | PARTIAL | BLOCKED
BRANCH: feat/sprint-27-electronics-ia
COMMIT: <hash> feat(ui): tra cứu serial + route hiding via feature flags (TIP-S27-08)

FILES CREATED:
  - src/app/(dashboard)/search/serial/page.tsx
  - src/app/(dashboard)/search/serial/[serial]/page.tsx
  - src/app/(dashboard)/search/serial/__tests__/page.test.tsx
  - src/app/(dashboard)/admin/feature-flags/page.tsx
  - src/app/(dashboard)/admin/feature-flags/__tests__/page.test.tsx
  - src/components/serial/serial-detail-card.tsx
  - src/components/serial/__tests__/serial-detail-card.test.tsx

FILES MODIFIED:
  - src/lib/feature-flags.ts                            (5 flag mới)
  - src/components/layout/sidebar-v2.tsx                (5 group + flag filter)
  - src/components/layout/__tests__/sidebar-v2.test.tsx (3 test mới)
  - .env.example                                        (5 var mới, default false)

FEATURE FLAGS ADDED:
  SHOW_FINANCE      = false (default)
  SHOW_AI_ML        = false
  SHOW_MULTITENANT  = false
  SHOW_MOBILE       = false
  SHOW_COMPLIANCE   = false

ROUTES VERIFIED EXIST (not deleted):
  /dashboard/finance/* — kept
  /dashboard/ai/*       — kept
  /dashboard/ml/*       — kept
  /dashboard/tenants/*  — kept (or N/A)
  /dashboard/subscription/* — kept (or N/A)
  /dashboard/mobile/*   — kept (or N/A)
  /dashboard/compliance/* — kept (or N/A)
  
  (Note: nếu route nào thực ra không tồn tại trong repo → ghi rõ. Vẫn thêm flag cho future).

TESTS:
  search/serial:        X/X pass
  serial-detail-card:   Y/Y pass
  admin/feature-flags:  Z/Z pass
  sidebar-v2 extended:  +3 pass (matrix admin × flag)
  Total: <sum> pass

TYPECHECK: 0 error
LINT: <delta>

MANUAL E2E:
  - Search serial IO1-V15-... → result render <500ms ✓
  - Deep link /dashboard/search/serial/<serial> → server render ✓
  - Children expand: N children listed ✓
  - Feature Flags page: 6 flag visible with description ✓
  - Toggle env SHOW_FINANCE=true + restart → cụm Tài chính visible cho admin ✓

DEVIATIONS: <list or "none">
SUGGESTIONS: <list or "none">
ISSUES POP UP: <list or "none">

Wave 5 done. Sprint 27 implementation complete. Sẵn sàng VERIFY phase.
```
