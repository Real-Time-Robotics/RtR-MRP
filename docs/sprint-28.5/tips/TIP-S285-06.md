# TIP-S285-06 · UX polish — skeleton loading + empty state CTA

## HEADER
- **ID:** TIP-S285-06
- **Branch:** `feat/sprint-28.5-adoption-polish`
- **Priority:** P2
- **Commit:** `feat(ui): UX polish — skeleton loading + empty state CTA (TIP-S285-06)`

## CONTEXT

6 page chính hiện dùng "Đang tải..." text (low-quality loading) + empty state thiếu icon/CTA. Polish nâng perceived performance + giảm confused.

## TASK

### 1) Skeleton loading components

`src/components/ui/skeleton-card.tsx` (nếu chưa có):
- Card với 3-4 div animate-pulse bg-muted.
- Match common card layout.

`src/components/ui/skeleton-table.tsx`:
- Row × 5 với cells animate-pulse.

`src/components/ui/skeleton-grid.tsx`:
- 7 column × 3 row (cho daily-plan weekly).

### 2) Empty state component

`src/components/ui/empty-state.tsx` reusable:

```tsx
interface EmptyStateProps {
  icon: LucideIcon;
  title: string;
  description: string;
  cta?: { label: string; href: string };
  secondaryCta?: { label: string; onClick: () => void };
}

export function EmptyState({ icon: Icon, title, description, cta, secondaryCta }: EmptyStateProps) {
  return (
    <div className="flex flex-col items-center justify-center py-16 px-4 text-center">
      <div className="rounded-full bg-muted p-6 mb-4">
        <Icon className="h-12 w-12 text-muted-foreground" />
      </div>
      <h3 className="text-lg font-semibold mb-2">{title}</h3>
      <p className="text-muted-foreground mb-6 max-w-md">{description}</p>
      <div className="flex gap-2">
        {cta && (
          <Button asChild>
            <Link href={cta.href}>{cta.label}</Link>
          </Button>
        )}
        {secondaryCta && (
          <Button variant="outline" onClick={secondaryCta.onClick}>
            {secondaryCta.label}
          </Button>
        )}
      </div>
    </div>
  );
}
```

### 3) Apply to 6 page

Replace "Đang tải..." + empty list:

| Page | Loading | Empty |
|---|---|---|
| `/production/daily-plan` | SkeletonGrid 7×3 | EmptyState icon=Calendar · "Chưa có plan" · CTA "Tạo plan đầu" |
| `/production/shift-entry` | SkeletonCard | EmptyState icon=Play · "Chưa được gán WO" · description "Quản đốc sẽ gán WO cho ca của bạn." |
| `/production/shift-report` | SkeletonTable | EmptyState icon=FileText · "Chưa có báo cáo ca" · description "Báo cáo tự generate sau khi ca kết thúc." |
| `/production/equipment` | SkeletonTable | EmptyState icon=Wrench · "Chưa có thiết bị" · CTA "Thêm thiết bị" |
| `/production/downtime` | SkeletonTable | EmptyState icon=AlertCircle · "Không có sự cố" · description "Hệ thống đang ổn." |
| `/production/report` | SkeletonCard × 4 | EmptyState icon=BarChart3 · "Chưa có dữ liệu" · description "Cần ≥ 1 ca hoàn thành để có report." |

### 4) Tests

Skip test cho từng page (đã có test cũ). Test 1 lần component:
`src/components/ui/__tests__/empty-state.test.tsx` ≥2:
1. Render với icon + title + description.
2. CTA click navigate.

## ACCEPTANCE

- [ ] tsc 0 error.
- [ ] 6 page replace loading + empty.
- [ ] Manual:
  - DB rỗng → 6 page thấy empty state với icon + CTA.
  - Slow network → skeleton hiện thay "Đang tải..." text.

## CONSTRAINTS

- Reuse `lucide-react` icon.
- Tailwind animate-pulse cho skeleton.
- Mỗi empty state có CTA dẫn tới action thật (không "Coming Sprint X" placeholder nữa — TIP-S285-03 seed data sẽ giúp khi có data thật).

## COMPLETION

```
COMMIT: <hash>
FILES CREATED:
  - src/components/ui/skeleton-card.tsx
  - src/components/ui/skeleton-table.tsx
  - src/components/ui/skeleton-grid.tsx
  - src/components/ui/empty-state.tsx
  - src/components/ui/__tests__/empty-state.test.tsx
FILES MODIFIED:
  - src/app/(dashboard)/production/{daily-plan,shift-entry,shift-report,equipment,downtime,report}/page.tsx
TESTS: 2/2 pass + existing tests still pass
```
