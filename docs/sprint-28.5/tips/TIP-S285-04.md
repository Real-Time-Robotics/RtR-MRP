# TIP-S285-04 · /admin/users live (list + assign role inline)

## HEADER
- **ID:** TIP-S285-04
- **Branch:** `feat/sprint-28.5-adoption-polish`
- **Depends on:** TIP-S285-03 (seed user data).
- **Priority:** P1
- **Commit:** `feat(admin): users list + role assignment UI (TIP-S285-04)`

## CONTEXT

Sprint 27.5 tạo skeleton `/admin/users` với placeholder. Admin (Lâm) cần quản lý user + role thật để gán role cho phòng. TIP này implement live.

API `/api/users/[id]/roles` Sprint 27 đã có (POST/DELETE assign/unassign). TIP này UI wrap.

## TASK

### 1) Page `/dashboard/admin/users/page.tsx`

Replace skeleton. Layout:

**Header:**
- "Người dùng" + count "N người dùng".
- Button "Mời người dùng" (mở dialog với email + role select). Optional Sprint 29.
- Filter: search by name/email, filter by role.

**Table:**
- Avatar · Tên · Email · Roles (badge list) · Status (active/disabled) · Last login · Action.
- Action: button "Sửa" → drawer detail.

**Drawer detail:**
- Read-only: id, name, email, createdAt.
- **Roles:** chip list 6 role hiện có (engineer, warehouse, production, procurement, admin, viewer):
  - Toggle on/off → API POST/DELETE `/api/users/[id]/roles`.
  - Toast "Đã gán role X" / "Đã bỏ role X".
- **Status:** toggle active/disabled (nếu API có `/api/users/[id]/status`).

### 2) API extend (nếu thiếu)

Verify Sprint 27 API:
- `GET /api/users` list users + roles. Nếu chưa có → tạo route mới.
- `GET /api/users/[id]` chi tiết + roles.
- `POST /api/users/[id]/roles` body `{roleCode}` assign.
- `DELETE /api/users/[id]/roles/[roleCode]` unassign.

Wrap `requireRole('admin')`.

### 3) Components

`src/components/admin/user-row.tsx` — row table.
`src/components/admin/role-chip-toggle.tsx` — chip toggle role inline.
`src/components/admin/user-detail-drawer.tsx` — drawer.

### 4) Tests

`src/app/api/users/__tests__/api.test.ts` (extend Sprint 27 nếu có):
1. `GET /api/users` non-admin → 403.
2. `GET /api/users` admin → list + roles.
3. `POST /api/users/[id]/roles` happy → 201, role added.

`src/app/(dashboard)/admin/users/__tests__/page.test.tsx` ≥3:
1. Render table 5 user mock.
2. Click row → drawer mở với 6 chip role.
3. Toggle chip → API call + toast.

## ACCEPTANCE

- [ ] tsc 0 error.
- [ ] Tests pass.
- [ ] Manual: login admin → `/admin/users` → list 5 user demo → click → toggle role engineer → user nhận role + sidebar refresh có cụm Kỹ thuật & R&D.

## CONSTRAINTS

- KHÔNG đụng schema (Role/UserRole đã có Sprint 27).
- KHÔNG cho phép gán role admin cho non-admin user qua UI này (chỉ admin DB-level làm).
- Reuse Shadcn Table, Drawer, Badge.

## COMPLETION

```
COMMIT: <hash>
FILES CREATED:
  - src/app/(dashboard)/admin/users/page.tsx (replace skeleton)
  - src/app/(dashboard)/admin/users/__tests__/page.test.tsx
  - src/components/admin/user-row.tsx
  - src/components/admin/role-chip-toggle.tsx
  - src/components/admin/user-detail-drawer.tsx
  - <API route extend nếu thiếu>
TESTS: X/X pass
```
