# TIP-S285-05 · Onboarding walkthrough lần đầu login

## HEADER
- **ID:** TIP-S285-05
- **Branch:** `feat/sprint-28.5-adoption-polish`
- **Depends on:** TIP-S285-02 (home empty state) + TIP-S285-03 (seed).
- **Priority:** P1
- **Commit:** `feat(ui): onboarding walkthrough — first login tour (TIP-S285-05)`

## CONTEXT

User mới login lần đầu → thấy app rỗng/lạ → không biết bắt đầu. Cần tour 3-4 step ngắn introduce key features.

## TASK

### 1) Library

Verify package có sẵn cho tour:
```bash
grep -E "intro\\.js|driver\\.js|tippy|joyride|onboardjs|shepherd" package.json
```

Nếu chưa có → recommend `react-joyride` (lightweight, headless, free) hoặc tự build với Shadcn Popover.

**Khuyến nghị:** tự build với Shadcn Popover + state — tránh thêm dep, control tốt hơn.

### 2) Tour state

Lưu state user đã xem tour qua:
- DB: `User.hasSeenOnboarding Boolean @default(false)`. Migration thêm field.
- Hoặc localStorage: `seen-onboarding-v1` (đơn giản hơn, không persist cross-device).

Khuyến nghị DB column — persist + admin có thể reset cho user.

### 3) Tour content (3-4 step)

**Step 1 — Welcome:**
- Anchor: top center home.
- Title: "Chào mừng đến RTR-MRP"
- Body: "Tour 1 phút giới thiệu app. Có thể skip bất kỳ lúc nào."
- Buttons: "Skip" · "Bắt đầu →"

**Step 2 — Sidebar v2:**
- Anchor: sidebar `<nav>`.
- Title: "Menu chính"
- Body: "9 cụm chức năng. Tuỳ role bạn thấy 5-9 cụm. Click expand để xem sub-menu."

**Step 3 — Quick Actions home:**
- Anchor: Quick Actions section trong home.
- Title: "Bắt đầu nhanh"
- Body: "Tạo work center, work order, plan trong 3 click."

**Step 4 — Mobile shift entry (operator only):**
- Chỉ show nếu user role = `production`.
- Anchor: sidebar item "Nhập ca".
- Title: "Operator dùng tablet"
- Body: "Trang `/production/shift-entry` mobile-first cho shop floor. Quét QR/serial nhanh."
- Button "Hoàn thành"

### 4) Component

`src/components/onboarding/tour-overlay.tsx`:
- Highlight anchor element (border + dim background).
- Popover bên cạnh anchor với content.
- Buttons Next/Prev/Skip.
- Step counter "1/4".

`src/components/onboarding/tour-provider.tsx`:
- Context provide tour state.
- Hook `useTour()` — start/skip/complete.

### 5) Trigger

Trong `src/app/(dashboard)/layout.tsx` (server) hoặc `dashboard-layout-client.tsx`:
```tsx
{!user.hasSeenOnboarding && <TourOverlay steps={tourSteps} userRole={userRoles[0]} />}
```

Sau Step cuối hoặc Skip → API `POST /api/users/me/seen-onboarding` set DB → tour không hiện lại.

### 6) Migration

```prisma
model User {
  ...
  hasSeenOnboarding Boolean @default(false)
}
```

Migrate diff + deploy.

### 7) Tests

`src/components/onboarding/__tests__/tour.test.tsx` ≥3:
1. Render với user.hasSeenOnboarding=false → tour visible.
2. hasSeenOnboarding=true → tour hidden.
3. Click "Skip" → API call + tour close.

## ACCEPTANCE

- [ ] Migration applied.
- [ ] tsc 0 error.
- [ ] Tests pass.
- [ ] Manual:
  - Tạo user mới (hoặc reset hasSeenOnboarding=false) → login → tour 4 step hiện.
  - Click Next/Prev work.
  - Skip → tour close + DB cập nhật.
  - Reload → tour không hiện.

## CONSTRAINTS

- KHÔNG dùng external library tour nặng.
- Tour không block app — Skip dễ tìm.
- A11y: ESC đóng tour, Tab navigate buttons.
- Mobile: tour responsive, popover không tràn viewport.

## COMPLETION

```
COMMIT: <hash>
FILES CREATED:
  - src/components/onboarding/tour-overlay.tsx
  - src/components/onboarding/tour-provider.tsx
  - src/components/onboarding/__tests__/tour.test.tsx
  - prisma/migrations/YYYYMMDD_add_hasSeenOnboarding/migration.sql
  - src/app/api/users/me/seen-onboarding/route.ts
FILES MODIFIED:
  - prisma/schema.prisma (User.hasSeenOnboarding)
  - src/components/layout/dashboard-layout-client.tsx (mount TourProvider)
TESTS: 3/3 pass
```
