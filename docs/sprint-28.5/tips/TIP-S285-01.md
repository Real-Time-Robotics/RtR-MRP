# TIP-S285-01 · Disable top header navigation

## HEADER
- **ID:** TIP-S285-01
- **Branch:** `feat/sprint-28.5-adoption-polish`
- **Priority:** P0
- **Commit:** `feat(ui): disable top header nav — sidebar v2 is single source (TIP-S285-01)`

## CONTEXT

Top header có 5 dropdown (HOME OPERATIONS PRODUCTION QUALITY ANALYTICS) song song với sidebar v2 9 cụm — confusing. Tool nội bộ chỉ cần 1 navigation. Sidebar v2 đã đầy đủ → ẩn top nav.

**File chính:** `src/components/layout/modern-header.tsx` + `header/header-nav.tsx` + `header-nav-data.tsx`.

## TASK

### 1) Hide top nav element trong header

`src/components/layout/modern-header.tsx`:
- Tìm `<HeaderNav />` import + render → comment out hoặc remove.
- Giữ logo + search bar + notifications + user menu.

### 2) Tuỳ chọn cleanup

- KHÔNG xoá file `header-nav.tsx`, `header-nav-data.tsx` (giữ rollback path).
- Có thể đặt feature flag `NEXT_PUBLIC_TOP_NAV=false` (default false), set env `=true` để bật lại nếu cần.

### 3) Test

`src/components/layout/__tests__/modern-header.test.tsx` (tạo mới hoặc extend):
1. Render header → không thấy "HOME" "OPERATIONS" "PRODUCTION" text.
2. Logo render OK.
3. User menu render OK.

## ACCEPTANCE

- [ ] tsc 0 error.
- [ ] lint OK.
- [ ] Manual: load `/home` → không thấy 5 dropdown top nav. Sidebar v2 là duy nhất.
- [ ] Logo + search + user menu vẫn hoạt động.

## CONSTRAINTS

- KHÔNG xoá file source.
- KHÔNG đụng sidebar v2.
- Reuse Tailwind class.

## COMPLETION

```
COMMIT: <hash>
FILES MODIFIED:
  - src/components/layout/modern-header.tsx
  - <feature flag file nếu thêm>
TESTS: X/X pass
DEVIATIONS: <list or "none">
```
