# TIP-S285-03 · Realistic seed demo data

## HEADER
- **ID:** TIP-S285-03
- **Branch:** `feat/sprint-28.5-adoption-polish`
- **Priority:** P0
- **Commit:** `chore(seed): realistic demo data for adoption (TIP-S285-03)`

## CONTEXT

TIP-S28-10 seed 5 user/role nhưng không seed business data thật. Phòng login vào thấy trống → block adoption. Cần seed realistic 1 ngày sản xuất mẫu.

## TASK

### 1) Script `prisma/seed-demo-production.ts`

Idempotent (upsert by unique key). CLI flag `--reset` nếu muốn xoá data demo cũ.

**Seed:**

**3 Work Centers:**
- `WC-SMT` SMT Line · type=MACHINE · capacityPerDay=480min
- `WC-ASM` Hand Assembly · type=ASSEMBLY · capacityPerDay=480min
- `WC-QC`  Testing & QC · type=TESTING · capacityPerDay=480min

**3 Shifts:**
- `SHIFT-S` Sáng (06:00-14:00)
- `SHIFT-C` Chiều (14:00-22:00)
- `SHIFT-D` Đêm (22:00-06:00)

**5 Equipment:**
- `EQ-SMT-01` SMT Pick & Place (workCenter=WC-SMT, status=running)
- `EQ-SMT-02` Reflow Oven (running)
- `EQ-ASM-01` Hand Assembly Bench (idle)
- `EQ-QC-01` ICT Tester (running)
- `EQ-QC-02` AOI Camera (maintenance, nextMaintenance=tomorrow)

**5 Work Orders:**
- `WO-DEMO-001` Hera IO1 v1.5 EBOX · qty=10 · status=DRAFT · dueDate=+3d
- `WO-DEMO-002` Hera Core v1.2 module · qty=20 · status=IN_PROGRESS · completedQty=8 · dueDate=+5d · assignedTo=op1
- `WO-DEMO-003` Speaker module v1.0 · qty=15 · status=COMPLETED · completedQty=15 · scrapQty=1 · dueDate=-1d (just done)
- `WO-DEMO-004` Battery pack v2.0 · qty=5 · status=DRAFT · dueDate=+7d
- `WO-DEMO-005` USB-C socket v0.1 · qty=50 · status=DRAFT · dueDate=-2d  ← LATE

**1 DailyProductionPlan hôm nay:**
- date=today, workCenter=WC-SMT, status=APPROVED
- Lines: WO-DEMO-002 (sequence=1, plannedQty=12, assignedTo=op1, est 08:00-12:00), WO-DEMO-001 (sequence=2, plannedQty=10, assignedTo=op2, est 13:00-17:00)

**1 ShiftAssignment hôm qua (đã end):**
- employee=op1, shift=SHIFT-S, date=yesterday
- actualStart, actualEnd đầy đủ
- 1 LaborEntry với quantityProduced=8, quantityScrapped=1, durationMinutes=420

**1 DowntimeRecord active:**
- workCenter=WC-QC, equipment=EQ-QC-02, type=MAINTENANCE
- startTime=2 giờ trước, endTime=null (đang mở)
- reason="Calibration scheduled maintenance"
- reportedBy=admin

**1 ShiftReport hôm qua:**
- date=yesterday, shift=SHIFT-S, workCenter=WC-SMT
- totalOutput=8, totalScrap=1, scrapRate=12.5
- Tự generate qua service nếu TIP-S28-06 đã có.

### 2) Idempotency

- `--dry-run` print count without writing.
- Default: upsert by code/woNumber/planNumber/etc. Re-run = 0 created.
- `--reset` flag xoá toàn bộ demo entry (filter `code LIKE 'WC-%' OR woNumber LIKE 'WO-DEMO-%'`) rồi tạo lại.

### 3) NPM script

`package.json`:
```json
"seed:demo": "tsx prisma/seed-demo-production.ts"
```

### 4) Tests

`prisma/__tests__/seed-demo-production.test.ts` (≥3):
1. Run trên DB sạch → tạo đúng count: 3 WC, 3 Shift, 5 Equipment, 5 WO, 1 Plan, 1 Assignment, 1 Downtime, 1 ShiftReport.
2. Re-run idempotent → 0 created.
3. `--dry-run` không ghi DB.

### 5) Document

`docs/sprint-28.5/SEED_DEMO.md`:
- Cách chạy: `npm run seed:demo`.
- List entity tạo.
- Cách reset: `npm run seed:demo -- --reset`.

## ACCEPTANCE

- [ ] tsc 0 error.
- [ ] Tests pass.
- [ ] `npm run seed:demo` trên DB local sạch → 25 entity tạo.
- [ ] `npm run seed:demo` lần 2 → 0 created.
- [ ] Sau seed, login admin → `/home` thấy KPI có data (không hero card empty), `/production/daily-plan` thấy 1 plan hôm nay, `/production/equipment` thấy 5 equipment, `/production/downtime` thấy 1 record active.

## CONSTRAINTS

- KHÔNG đụng schema.
- KHÔNG đụng entity production thật của user (chỉ dùng prefix `WC-`, `WO-DEMO-`, `SHIFT-`, etc.).
- Reuse Prisma singleton.

## COMPLETION

```
COMMIT: <hash>
FILES CREATED:
  - prisma/seed-demo-production.ts
  - prisma/__tests__/seed-demo-production.test.ts
  - docs/sprint-28.5/SEED_DEMO.md
FILES MODIFIED:
  - package.json (add seed:demo script)
TESTS: 3/3 pass
SEED RESULT: <count breakdown>
```
