# TIP-S28-02 · Sidebar v2 cụm "Sản xuất" + role-gate

## HEADER
- **ID:** TIP-S28-02
- **Branch:** `feat/sprint-28-production-shop-floor`
- **Depends on:** Sprint 27 sidebar v2 (`fc9e6e0`).
- **Runs parallel with:** TIP-S28-01 (schema — không đụng cùng file).
- **Blocks:** không TIP nào trực tiếp, nhưng Wave 2-3 page sẽ link từ sidebar.
- **Priority:** P0
- **REQ cover:** infrastructure cho mọi REQ phòng Sản xuất.
- **Commit message:** `feat(ui): sidebar cụm Sản xuất 6 sub-menu role-gated (TIP-S28-02)`

## CONTEXT

**Working dir:** `/Users/os/RtR/rtr-mrp-pr`

**Tại sao TIP này:**
Sidebar v2 Sprint 27 chỉ có "Gia công" trong cụm "Vận hành" cho production role. Phòng Sản xuất Sprint 28 cần cụm riêng đầy đủ 6 chức năng. Phải role-gate `production + admin` (operator/supervisor/quản đốc/bảo trì/trưởng phòng đều có role `production` — phân biệt qua sub-page permission).

**Key file đọc trước:**
- `src/components/layout/sidebar-v2.tsx` — group config + filter logic Sprint 27.
- `src/components/layout/__tests__/sidebar-v2.test.tsx` — pattern test role × group matrix.
- `src/lib/auth/rbac.ts` — `RoleCode`.

## TASK

### 1) Thêm group "Sản xuất" vào `SIDEBAR_GROUPS`

Trong `src/components/layout/sidebar-v2.tsx`:

```ts
{
  id: 'production',
  label: 'Sản xuất',
  icon: Factory,
  roles: ['production', 'admin'],
  // featureFlag: undefined (always visible cho production+admin)
  items: [
    { href: '/dashboard/production/daily-plan',    label: 'Kế hoạch ngày' },
    { href: '/dashboard/production/shift-report',  label: 'Báo cáo ca' },
    { href: '/dashboard/production/shift-entry',   label: 'Nhập ca (Operator)', roles: ['production', 'admin'] },
    { href: '/dashboard/production/equipment',     label: 'Thiết bị' },
    { href: '/dashboard/production/downtime',      label: 'Sự cố máy' },
    { href: '/dashboard/production/report',        label: 'Báo cáo sản xuất' },
  ],
}
```

Vị trí trong array: ngay sau "Vận hành" trước "Tra cứu" (giữ ordering logical workflow).

Import `Factory` từ `lucide-react` (đã có dep).

### 2) Skeleton 6 page

Tạo file skeleton mỗi route — heading + 1 dòng mô tả + back link:

- `src/app/(dashboard)/production/daily-plan/page.tsx`
- `src/app/(dashboard)/production/shift-report/page.tsx`
- `src/app/(dashboard)/production/shift-entry/page.tsx`
- `src/app/(dashboard)/production/equipment/page.tsx` (route đã có Sprint trước? grep verify; nếu có thì không tạo skeleton)
- `src/app/(dashboard)/production/downtime/page.tsx`
- `src/app/(dashboard)/production/report/page.tsx`

Pattern skeleton:
```tsx
export default function DailyPlanPage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-semibold mb-2">Kế hoạch ngày</h1>
      <p className="text-muted-foreground">Quản đốc lập kế hoạch sản xuất theo work center.</p>
      <p className="text-sm text-muted-foreground mt-4">TIP-S28-03 sẽ implement đầy đủ.</p>
    </div>
  );
}
```

**Lưu ý:** route `/dashboard/production/equipment`, `/oee`, `/scheduler`, `/work-centers` đã có Sprint trước. Sidebar v2 link tới page hiện có, KHÔNG override. Skeleton chỉ tạo khi route chưa tồn tại — verify qua grep.

### 3) Cập nhật `dashboard-layout-client.tsx` nếu cần

Verify `SidebarV2` đã pass đủ `userRoles` (Sprint 27 đã wire). Không đụng nếu OK.

### 4) Tests

**`src/components/layout/__tests__/sidebar-v2.test.tsx`** mở rộng:

Thêm test:
- Engineer role → KHÔNG thấy cụm "Sản xuất".
- Production role → thấy cụm "Sản xuất" 6 sub-menu.
- Warehouse role → KHÔNG thấy cụm "Sản xuất".
- Admin role → thấy cụm "Sản xuất".
- Production + admin combined → thấy cụm "Sản xuất" 1 lần (no duplicate).
- Operator (only production role) → 6 sub-menu visible.

Total +6 test mới (group total từ Sprint 27 13 → 19 test).

Test "operations" cụm — verify "Gia công" vẫn visible cho production role (regression).

### 5) Update report page

`docs/sprint-28/reports/TIP-S28-02-REPORT.md` ghi role × group matrix mở rộng:

```
admin       → 10 groups (9 Sprint 27 + Sản xuất)
engineer    →  6 groups (không có Sản xuất, không có Mua hàng/Kho)
warehouse   →  6 groups
production  →  6 groups (5 Sprint 27 chung + Sản xuất)
procurement →  6 groups
viewer      →  5 groups
```

## ACCEPTANCE

- [ ] `tsc --noEmit` 0 error mới.
- [ ] `lint` không tăng warning.
- [ ] Sidebar test +6 pass (total ≥ 19).
- [ ] Manual UI smoke (`npm run dev`):
  - Login user role `production` → thấy cụm "Sản xuất" với 6 sub-menu.
  - Login `engineer` → KHÔNG thấy.
  - Click 6 sub-menu → load page (skeleton hoặc page có sẵn), không 404.
  - Cụm "Sản xuất" expand/collapse keyboard (Tab + Enter).

## CONSTRAINTS

- KHÔNG xoá cụm cũ.
- KHÔNG đụng SidebarLegacy.
- KHÔNG fetch role client-side (server-only đã wire Sprint 27).
- Reuse `Factory` icon từ `lucide-react`.
- A11y giữ chuẩn Sprint 27 (aria-expanded, role="navigation").
- Mobile responsive (sidebar collapse ở 768px breakpoint — Shadcn sidebar đã handle).

## COMPLETION REPORT FORMAT

```
STATUS: DONE | PARTIAL | BLOCKED
BRANCH: feat/sprint-28-production-shop-floor
COMMIT: <hash> feat(ui): sidebar cụm Sản xuất 6 sub-menu role-gated (TIP-S28-02)

FILES CREATED:
  - src/app/(dashboard)/production/daily-plan/page.tsx       (skeleton)
  - src/app/(dashboard)/production/shift-report/page.tsx     (skeleton)
  - src/app/(dashboard)/production/shift-entry/page.tsx      (skeleton)
  - src/app/(dashboard)/production/downtime/page.tsx         (skeleton)
  - src/app/(dashboard)/production/report/page.tsx           (skeleton)
  - <equipment đã có sẵn nếu yes thì SKIP>

FILES MODIFIED:
  - src/components/layout/sidebar-v2.tsx
  - src/components/layout/__tests__/sidebar-v2.test.tsx     (+6 test)

ROLE × GROUP MATRIX (after Sprint 28 TIP-02):
  admin       → 10 groups
  engineer    →  6 groups
  warehouse   →  6 groups
  production  →  6 groups (Sản xuất xuất hiện)
  procurement →  6 groups
  viewer      →  5 groups

TESTS: ≥19/19 pass (+6 mới)

TYPECHECK: 0 error
LINT: no new warning

DEVIATIONS: <list or "none">
SUGGESTIONS: <list or "none">

Wave 1 done sau TIP-01 + TIP-02. Chờ Wave 2 TIP.
```
